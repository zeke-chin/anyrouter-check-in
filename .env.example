# AnyRouter 账号配置
# 格式为 JSON 数组，必须写在一行内（.env 不支持未加引号的多行值）
# 支持字段：name(可选), provider(可选,默认anyrouter), cookies(必填), api_user(必填)
ANYROUTER_ACCOUNTS=[{"cookies":{"session":"你的session值"},"api_user":"你的api_user值"}]

# 多账号 / 多 Provider 示例（写在一行）：
# ANYROUTER_ACCOUNTS=[{"name":"AnyRouter主账号","provider":"anyrouter","cookies":{"session":"abc123"},"api_user":"user123"},{"name":"HotaruAPI账号","provider":"hotaruapi","cookies":{"session":"xyz789"},"api_user":"5677"}]

# 可选：自定义 Provider 配置
# 格式为 JSON 对象（不是数组），key 为 provider 名称，value 为配置
# 支持字段：domain(必填), sign_in_path(签到接口路径), login_path, user_info_path, api_user_key, bypass_method
# sign_in_path 默认为 /api/user/sign_in，路径不同时需覆盖；设为 null 表示查询用户信息时自动完成签到
# PROVIDERS={"hotaruapi":{"domain":"https://hotaruapi.com","sign_in_path":"/api/user/checkin"}}

# 可选：通知配置
# DINGDING_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=xxx
# EMAIL_USER=your_email@example.com
# EMAIL_PASS=your_password
# EMAIL_TO=recipient@example.com
# PUSHPLUS_TOKEN=your_pushplus_token
# SERVERPUSHKEY=your_server_pushkey
# FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
# WEIXIN_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx
